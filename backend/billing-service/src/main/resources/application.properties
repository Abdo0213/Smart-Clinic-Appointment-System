spring.application.name=billing-service
server.port=8087

# Database Configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# External Services
services.patient.url=${PATIENT_SERVICE_URL:http://localhost:8083/patients}
services.appointment.url=${APPOINTMENT_SERVICE_URL:http://localhost:8084/appointments}
services.visit.url=${VISIT_SERVICE_URL:http://localhost:8085/visits}

# Logging
logging.level.com.clinic.billing_service=DEBUG
